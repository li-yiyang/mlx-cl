;;;; mlx-cl.el ---- Base  -*- lexical-binding: t; -*-

;; Not known what to do, just leave it be.

(defgroup mlx-cl ()
  "MLX-CL dev tools. "
  :prefix "mlx-cl")

(defcustom mlx-cl-root-dir "~/common-lisp/mlx-cl/"
  "Path of MLX-CL source root. "
  :type 'directory)

(defcustom mlx-cl-test-dir "~/common-lisp/mlx-cl/test/"
  "Path of MLX-CL test dir. "
  :type 'directory)

(provide 'mlx-cl)

;;;; mlx-cl.el ends here
